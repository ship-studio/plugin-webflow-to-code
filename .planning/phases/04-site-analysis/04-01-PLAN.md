---
phase: 04-site-analysis
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/analysis/types.ts
  - src/analysis/parse.ts
  - src/analysis/parse.test.ts
  - src/analysis/webflow.ts
  - src/analysis/webflow.test.ts
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03, WFLW-01, WFLW-02, WFLW-03]

must_haves:
  truths:
    - "discoverHtmlPages filters ZipManifest entries to only .html files, excluding directories and __MACOSX"
    - "parsePage extracts title, route, sections, Webflow components, IX2 interactions, CMS template flag from HTML"
    - "inferRoute converts filenames to URL routes including subdirectory paths and CMS slug patterns"
    - "detectCmsTemplate returns true for detail_ prefix, w-dyn-bind-empty class, or title starting with |"
    - "WEBFLOW_COMPONENT_REGISTRY maps all 10 known w-* classes to labels and migration notes"
    - "detectComponents finds all w-* classes in a page and returns ComponentEntry[] with counts"
    - "detectInteractions returns true when data-animation or data-easing attributes are present"
  artifacts:
    - path: "src/analysis/types.ts"
      provides: "SiteAnalysis, PageInfo, SectionItem, ComponentEntry, ComponentDef, SharedLayout type definitions"
      exports: ["PageInfo", "SectionItem", "ComponentEntry", "ComponentDef", "SharedLayout", "SiteAnalysis"]
    - path: "src/analysis/parse.ts"
      provides: "discoverHtmlPages, inferRoute, parsePage, detectCmsTemplate, inferSectionLabel"
      exports: ["discoverHtmlPages", "inferRoute", "parsePage", "detectCmsTemplate", "inferSectionLabel"]
    - path: "src/analysis/webflow.ts"
      provides: "WEBFLOW_COMPONENT_REGISTRY, detectComponents, detectInteractions"
      exports: ["WEBFLOW_COMPONENT_REGISTRY", "detectComponents", "detectInteractions"]
    - path: "src/analysis/parse.test.ts"
      provides: "Unit tests for PAGE-01, PAGE-02, WFLW-03"
    - path: "src/analysis/webflow.test.ts"
      provides: "Unit tests for WFLW-01, WFLW-02"
  key_links:
    - from: "src/analysis/parse.ts"
      to: "src/analysis/types.ts"
      via: "imports PageInfo, SectionItem, ComponentEntry"
      pattern: "import.*from.*types"
    - from: "src/analysis/parse.ts"
      to: "src/analysis/webflow.ts"
      via: "imports detectComponents, detectInteractions"
      pattern: "import.*from.*webflow"
---

<objective>
Build the core analysis functions: type definitions, per-page HTML parsing (title, route, sections, CMS detection), and Webflow component/interaction detection with static registry.

Purpose: These pure functions form the foundation for site analysis — every other plan depends on PageInfo and the component registry.
Output: `src/analysis/types.ts`, `src/analysis/parse.ts`, `src/analysis/webflow.ts` with full test coverage.
</objective>

<execution_context>
@/Users/juliangalluzzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliangalluzzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-site-analysis/04-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From src/types.ts:
```typescript
export interface Shell {
  exec(command: string, args: string[], options?: { timeout?: number }): Promise<{
    exit_code: number;
    stdout: string;
    stderr: string;
  }>;
}
```

From src/zip/types.ts:
```typescript
export interface ZipManifest {
  fileCount: number;
  entries: string[];
}
```

From src/assets/types.ts:
```typescript
export interface AssetManifest {
  images: ImageEntry[];
  videos: VideoEntry[];
  fonts: FontEntry[];
  cssFiles: string[];
  totalCopied: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types and page parsing with tests (PAGE-01, PAGE-02, PAGE-03, WFLW-03)</name>
  <files>src/analysis/types.ts, src/analysis/parse.ts, src/analysis/parse.test.ts</files>
  <read_first>src/types.ts, src/zip/types.ts, src/assets/types.ts, src/zip/discover.test.ts</read_first>
  <behavior>
    - discoverHtmlPages(['index.html', 'css/', 'about.html', '__MACOSX/foo.html', 'images/']) returns ['index.html', 'about.html']
    - inferRoute('index.html') returns '/'
    - inferRoute('about.html') returns '/about'
    - inferRoute('legal/terms-of-service.html') returns '/legal/terms-of-service'
    - inferRoute('detail_post.html') returns '/post/[slug]'
    - detectCmsTemplate('detail_post.html', docWithNormalTitle) returns true (filename signal)
    - detectCmsTemplate('about.html', docWithDynBindEmpty) returns true (w-dyn-bind-empty signal)
    - detectCmsTemplate('blog.html', docWithPipeTitle) returns true (title starts with |)
    - detectCmsTemplate('about.html', normalDoc) returns false
    - inferSectionLabel('section_hero padding-large') returns 'hero'
    - inferSectionLabel('footer_component') returns 'footer'
    - inferSectionLabel('random-class') returns 'section' (fallback)
    - parsePage extracts title from HTML with DOMParser
    - parsePage extracts data-wf-page attribute
    - parsePage extracts sections matching section_*, header_*, footer_* class patterns
    - parsePage detects nav data-w-id and footer data-w-id for shared layout detection
    - parsePage marks isUtilityPage true for 401.html, 404.html, style-guide.html
    - PAGE-03 is satisfied by existing AssetManifest.cssFiles — no new code needed, just confirm in test comments
  </behavior>
  <action>
    **TDD: RED phase first.**

    1. Create `src/analysis/types.ts` with these exact types:
       ```typescript
       export interface PageInfo {
         filename: string;
         route: string;
         title: string;
         wfPageId: string;
         isCmsTemplate: boolean;
         isUtilityPage: boolean;
         sections: SectionItem[];
         webflowComponents: ComponentEntry[];
         hasIx2Interactions: boolean;
         navWfId: string | null;
         footerWfId: string | null;
       }

       export interface SectionItem {
         tag: string;
         className: string;
         label: string;
       }

       export interface ComponentEntry {
         wClass: string;
         label: string;
         count: number;
         migration: string;
       }

       export interface ComponentDef {
         label: string;
         migration: string;
       }

       export interface SharedLayout {
         hasSharedNav: boolean;
         navWfId: string | undefined;
         hasSharedFooter: boolean;
         footerWfId: string | undefined;
         confidence: 'high' | 'medium';
       }

       export interface SiteAnalysis {
         pages: PageInfo[];
         sharedLayout: SharedLayout;
         contentPageCount: number;
         cmsTemplateCount: number;
         allWebflowComponents: string[];
       }
       ```

    2. Create `src/analysis/parse.test.ts` with `// @vitest-environment jsdom` header. Write failing tests for ALL behaviors listed above. Use DOMParser to create test documents inline. Follow the Mock Shell pattern from `src/zip/discover.test.ts` — create a `mockShell` that returns base64-encoded HTML for `bash -c base64 < ...` calls.

    3. Create `src/analysis/parse.ts` with exported functions:
       - `discoverHtmlPages(entries: string[]): string[]` — filter to .html, exclude directories (ending '/') and __MACOSX entries
       - `inferRoute(filename: string): string` — strip .html, handle index→/, detail_X→/X/[slug], subdirectories
       - `detectCmsTemplate(filename: string, doc: Document): boolean` — three-signal: detail_ prefix, .w-dyn-bind-empty, title startsWith('|')
       - `inferSectionLabel(className: string): string` — extract from section_*, footer_*, header_* prefixes, fallback to 'section'
       - `parsePage(shell: Shell, htmlPath: string, filename: string): Promise<PageInfo>` — reads HTML via `shell.exec('bash', ['-c', "base64 < '" + htmlPath + "'"])`, decodes with `atob()`, parses with DOMParser, extracts all PageInfo fields. Calls `detectCmsTemplate`, `inferRoute`, `inferSectionLabel`. Imports `detectComponents` and `detectInteractions` from `./webflow.ts`. Detects utility pages: filename matches `401.html`, `404.html`, or contains `style-guide`.
       - Section extraction: `doc.querySelectorAll('section[class*="section_"], header[class*="section_"], header[class*="header_"], div[class*="section_"], footer[class*="footer_"]')`
       - Nav wfId: `doc.querySelector('.w-nav')?.getAttribute('data-w-id') ?? null`
       - Footer wfId: `doc.querySelector('footer')?.getAttribute('data-w-id') ?? doc.querySelector('[class*="footer_"]')?.getAttribute('data-w-id') ?? null`

    **GREEN phase:** Run tests, iterate until all pass.

    Note: PAGE-03 (CSS referenced by path) is already satisfied by `AssetManifest.cssFiles` from Phase 3. Add a comment in parse.test.ts: `// PAGE-03: CSS file references handled by AssetManifest.cssFiles (Phase 3) — no analysis-phase code needed`
  </action>
  <verify>
    <automated>npx vitest run src/analysis/parse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function discoverHtmlPages" src/analysis/parse.ts` returns 1
    - `grep -c "export function inferRoute" src/analysis/parse.ts` returns 1
    - `grep -c "export function parsePage" src/analysis/parse.ts` returns 1
    - `grep -c "export function detectCmsTemplate" src/analysis/parse.ts` returns 1
    - `grep -c "export function inferSectionLabel" src/analysis/parse.ts` returns 1
    - `grep -c "export interface PageInfo" src/analysis/types.ts` returns 1
    - `grep -c "export interface SiteAnalysis" src/analysis/types.ts` returns 1
    - `grep -c "@vitest-environment jsdom" src/analysis/parse.test.ts` returns 1
    - `grep -c "PAGE-03" src/analysis/parse.test.ts` returns at least 1 (comment confirming coverage)
    - `npx vitest run src/analysis/parse.test.ts` exits 0
  </acceptance_criteria>
  <done>
    All pure parsing functions implemented and tested. discoverHtmlPages filters entries. inferRoute handles all filename patterns including subdirectories and CMS detail_ pages. detectCmsTemplate catches all three signals. parsePage produces complete PageInfo from HTML via DOMParser. inferSectionLabel extracts semantic labels from Webflow class conventions. Utility pages (401, 404, style-guide) flagged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Webflow component registry and interaction detection (WFLW-01, WFLW-02)</name>
  <files>src/analysis/webflow.ts, src/analysis/webflow.test.ts</files>
  <read_first>src/analysis/types.ts, src/analysis/parse.test.ts</read_first>
  <behavior>
    - WEBFLOW_COMPONENT_REGISTRY has entries for: w-nav, w-dropdown, w-slider, w-tabs, w-form, w-lightbox, w-embed, w-richtext, w-background-video, w-dyn-list
    - Each registry entry has label (string) and migration (string)
    - detectComponents(doc) given HTML with two .w-nav and one .w-tabs returns [{wClass:'w-nav', label:'Navbar', count:2, migration:...}, {wClass:'w-tabs', label:'Tab Switcher', count:1, migration:...}]
    - detectComponents(doc) given HTML with no w-* classes returns []
    - detectInteractions(doc) returns true when element has data-animation attribute
    - detectInteractions(doc) returns true when element has data-easing attribute
    - detectInteractions(doc) returns true when element has data-duration-in attribute
    - detectInteractions(doc) returns false when no interaction attributes present
  </behavior>
  <action>
    **TDD: RED phase first.**

    1. Create `src/analysis/webflow.test.ts` with `// @vitest-environment jsdom` header. Write failing tests for ALL behaviors above. Create test HTML documents with DOMParser inline.

    2. Create `src/analysis/webflow.ts`:
       - `WEBFLOW_COMPONENT_REGISTRY: Record<string, ComponentDef>` — static map with exactly these 10 entries:
         - `w-nav`: label "Navbar", migration "Replace with semantic <nav> + mobile hamburger. w-nav JS handles collapse — rebuild with CSS/JS toggle."
         - `w-dropdown`: label "Dropdown", migration "Replace with <details>/<summary> or CSS :hover + focus-within pattern. Webflow JS drives open/close."
         - `w-slider`: label "Slider", migration "Replace with a carousel library (Embla, Swiper) or CSS scroll snap. w-slider JS is non-portable."
         - `w-tabs`: label "Tab Switcher", migration "Replace with accessible tab pattern (ARIA role=\"tablist\"). Webflow JS drives panel switching."
         - `w-form`: label "Form", migration "Form HTML is usable; replace Webflow form handling backend. Use Resend, Formspree, or server actions."
         - `w-lightbox`: label "Lightbox", migration "Replace with <dialog> element or a lightbox library. Webflow JS drives open/close/media display."
         - `w-embed`: label "HTML Embed", migration "Custom HTML embed — review contents. May contain third-party scripts; preserve as-is or integrate natively."
         - `w-richtext`: label "Rich Text", migration "CMS-bound rich text area. Wrap in prose CSS (e.g., Tailwind @typography) for correct rendering."
         - `w-background-video`: label "Background Video", migration "Replace with <video autoplay muted loop playsinline>. Video URLs in data-video-urls attr point to Webflow CDN — re-host the local copies."
         - `w-dyn-list`: label "CMS Collection List", migration "CMS-driven list. Replace with static data array or API fetch. No data exported in zip."

       - `detectComponents(doc: Document): ComponentEntry[]` — scan all elements, collect class lists, match against registry keys using `className.split(/\s+/).some(cls => cls === key || cls.startsWith(key + '-'))` pattern. BUT only match exact `w-nav` not `w-nav-brand` etc. Actually, match elements that have the class `w-nav` (use `doc.querySelectorAll('[class*="w-nav"]')` then verify exact class presence). Simpler approach: iterate registry keys, for each key do `doc.querySelectorAll('.' + key)` and count. This is cleaner.

       - `detectInteractions(doc: Document): boolean` — check `doc.querySelector('[data-animation], [data-easing], [data-duration-in], [data-duration-out], [data-collapse]') !== null`

    **GREEN phase:** Run tests, iterate until all pass.
  </action>
  <verify>
    <automated>npx vitest run src/analysis/webflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WEBFLOW_COMPONENT_REGISTRY" src/analysis/webflow.ts` returns at least 1
    - `grep -c "export function detectComponents" src/analysis/webflow.ts` returns 1
    - `grep -c "export function detectInteractions" src/analysis/webflow.ts` returns 1
    - `grep -c "w-nav" src/analysis/webflow.ts` returns at least 1
    - `grep -c "w-dyn-list" src/analysis/webflow.ts` returns at least 1
    - `grep -c "@vitest-environment jsdom" src/analysis/webflow.test.ts` returns 1
    - `npx vitest run src/analysis/webflow.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Static WEBFLOW_COMPONENT_REGISTRY with all 10 w-* entries. detectComponents scans a parsed Document and returns ComponentEntry[] with counts. detectInteractions detects IX2 attribute presence. All tests green.
  </done>
</task>

</tasks>

<verification>
npx vitest run src/analysis/
All analysis tests pass. Types compile. No regressions in existing tests (npx vitest run).
</verification>

<success_criteria>
- types.ts exports all 6 interfaces (PageInfo, SectionItem, ComponentEntry, ComponentDef, SharedLayout, SiteAnalysis)
- parse.ts has 5 exported functions covering page discovery, route inference, CMS detection, section labeling, full page parsing
- webflow.ts has 10-entry component registry + 2 detection functions
- All tests pass with jsdom environment
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-site-analysis/04-01-SUMMARY.md`
</output>
