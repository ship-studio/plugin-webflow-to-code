---
phase: 08-session-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MigrationProgress.tsx
  - src/plan/resumePrompt.ts
  - src/plan/resumePrompt.test.ts
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - HAND-03
must_haves:
  truths:
    - "A 'Continue Migration' button appears in the progress view"
    - "Clicking Continue Migration copies a resume prompt to clipboard"
    - "The resume prompt tells the agent to read migration-plan.json and brief.md, then continue"
    - "Button shows 'Copied!' feedback for 2 seconds after click"
    - "HAND-02 and HAND-03 are already satisfied — the skeleton plan file is written during brief generation, so progress view at 0% is the waiting state and the transition is instant"
  artifacts:
    - path: "src/plan/resumePrompt.ts"
      provides: "Pure function generating resume prompt text"
      exports: ["buildResumePrompt"]
    - path: "src/plan/resumePrompt.test.ts"
      provides: "Tests for resume prompt generation"
    - path: "src/components/MigrationProgress.tsx"
      provides: "Continue Migration button with clipboard copy"
  key_links:
    - from: "src/components/MigrationProgress.tsx"
      to: "src/plan/resumePrompt.ts"
      via: "import buildResumePrompt"
      pattern: "buildResumePrompt"
    - from: "src/components/MigrationProgress.tsx"
      to: "src/brief/io.ts"
      via: "import copyToClipboard"
      pattern: "copyToClipboard"
---

<objective>
Add a "Continue Migration" button to the MigrationProgress component that copies a resume prompt to clipboard, enabling users to pause and resume agent sessions with one click.

Purpose: This is the final piece of the v1.1 milestone — users can hand off context between agent sessions without manually composing prompts.
Output: Resume prompt generator, updated MigrationProgress component with copy button.
</objective>

<execution_context>
@/Users/juliangalluzzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliangalluzzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-session-handoff/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/plan/types.ts:
```typescript
export type PlanStatus = 'pending' | 'in-progress' | 'complete';
export interface PlanItem {
  name: string;
  type: 'shared' | 'page' | 'section' | 'component';
  status: PlanStatus;
  notes?: string;
  children?: PlanItem[];
}
export interface MigrationPlan {
  version: '1.0';
  generatedAt: string;
  items: PlanItem[];
}
```

From src/brief/io.ts:
```typescript
export async function copyToClipboard(
  shell: ShellLike,
  markdown: string,
): Promise<void>;
```

From src/components/MigrationProgress.tsx:
```typescript
interface MigrationProgressProps {
  shell: { exec(cmd: string, args: string[]): Promise<{ exit_code: number; stdout: string; stderr: string }> };
  projectPath: string;
}
export function MigrationProgress({ shell, projectPath }: MigrationProgressProps): JSX.Element;
```

MigrationProgress is rendered in MainView.tsx at line 320, inside the `step.kind === 'done' && step.briefResult` branch, as a sibling below the brief results div.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resume prompt generator with tests</name>
  <files>src/plan/resumePrompt.ts, src/plan/resumePrompt.test.ts</files>
  <read_first>
    - src/plan/types.ts (MigrationPlan, PlanItem types)
    - src/brief/io.ts (pattern for shell-based operations)
  </read_first>
  <behavior>
    - buildResumePrompt(projectPath) returns a string containing the plan file path (.shipstudio/migration-plan.json)
    - buildResumePrompt(projectPath) returns a string containing the brief file path (.shipstudio/assets/brief.md)
    - The prompt instructs the agent to read both files and continue from where it left off
    - The prompt includes the projectPath in the file paths
  </behavior>
  <action>
Create `src/plan/resumePrompt.ts` with a pure function `buildResumePrompt(projectPath: string): string` that returns a resume prompt. The prompt should be concise and agent-agnostic:

```
Read the migration plan at {projectPath}/.shipstudio/migration-plan.json and the brief at {projectPath}/.shipstudio/assets/brief.md. Continue the migration from where you left off — update each item's status in the plan file as you complete it.
```

Create `src/plan/resumePrompt.test.ts` using vitest (follow existing test patterns in src/plan/). Test that the output contains both file paths and the key instruction to continue.

Export `buildResumePrompt` from the module.
  </action>
  <verify>
    <automated>npx vitest run src/plan/resumePrompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - buildResumePrompt is a pure function, no async, no shell dependency
    - Tests pass confirming both file paths appear in output
    - Prompt is under 500 characters (lightweight, not full brief)
  </acceptance_criteria>
  <done>Resume prompt generator exists with passing tests, exported and ready for UI consumption</done>
</task>

<task type="auto">
  <name>Task 2: Add Continue Migration button to MigrationProgress</name>
  <files>src/components/MigrationProgress.tsx</files>
  <read_first>
    - src/components/MigrationProgress.tsx (full current component)
    - src/views/MainView.tsx (lines 290-325 — how MigrationProgress is rendered, copy pattern with copied state)
    - src/brief/io.ts (copyToClipboard function)
    - src/plan/resumePrompt.ts (buildResumePrompt from Task 1)
  </read_first>
  <action>
Update `MigrationProgress` component to add a "Continue Migration" button at the bottom of the progress view (after the item list).

1. Import `buildResumePrompt` from `../plan/resumePrompt` and `copyToClipboard` from `../brief/io`
2. Add `const [resumeCopied, setResumeCopied] = useState(false)` state
3. Add a click handler that:
   - Calls `buildResumePrompt(projectPath)` to get the prompt text
   - Calls `copyToClipboard(shell, promptText)` to copy it
   - Sets `resumeCopied` to true, then resets after 2000ms via setTimeout
4. Render the button below the orderedItems map, using the `wf2c-btn-ghost` class for styling consistency with the Start Over button
5. Button text: `resumeCopied ? 'Copied!' : 'Continue Migration'`
6. The button should always be visible when the plan is loaded (not gated on progress > 0) — users may want to resume even at 0%

Do NOT add any "waiting for plan" UI — per user decision, the skeleton plan exists immediately after brief generation, so the 0% progress view IS the waiting state. HAND-02 and HAND-03 are satisfied by the existing Phase 6 behavior.
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Continue Migration button renders below the item list in MigrationProgress
    - Clicking copies resume prompt to clipboard via copyToClipboard
    - Shows "Copied!" for 2 seconds then reverts to "Continue Migration"
    - Uses wf2c-btn-ghost class for consistent styling
    - No "waiting for plan" UI added — existing behavior is correct
    - Build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>"Continue Migration" button visible in progress view, copies resume prompt on click with feedback</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/plan/resumePrompt.test.ts` — resume prompt tests pass
2. `npx vite build` — no TypeScript or build errors
3. Visual check: MigrationProgress shows "Continue Migration" ghost button below item list
4. Click test: Button copies prompt containing both file paths, shows "Copied!" feedback
</verification>

<success_criteria>
- HAND-01: "Continue Migration" button copies resume prompt pointing to plan file + brief
- HAND-02: Satisfied by Phase 6 — skeleton plan exists immediately, 0% progress view is the waiting state
- HAND-03: Satisfied by Phase 6 — auto-transition is instant since plan file written in same pipeline step
- All three HAND requirements addressed, v1.1 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-session-handoff/08-01-SUMMARY.md`
</output>
