---
phase: 01-plugin-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin.json
  - package.json
  - vite.config.ts
  - tsconfig.json
  - .gitignore
  - src/index.tsx
  - src/types.ts
  - src/context.ts
  - src/styles.ts
  - src/components/Modal.tsx
  - src/views/MainView.tsx
  - src/zip/.gitkeep
  - src/assets/.gitkeep
  - src/analysis/.gitkeep
  - src/brief/.gitkeep
  - dist/index.js
autonomous: false
requirements: [INFR-01, INFR-02]

must_haves:
  truths:
    - "Plugin appears in Ship Studio toolbar when linked as dev plugin"
    - "Clicking the toolbar button opens a modal with mode selector cards and disabled file picker"
    - "Modal closes on Escape key, overlay click, or close action"
    - "dist/index.js exists, is committed, and contains no bundled React"
    - "Plugin file structure matches plugin-starter conventions"
  artifacts:
    - path: "plugin.json"
      provides: "Plugin manifest with id webflow-to-code"
      contains: "webflow-to-code"
    - path: "vite.config.ts"
      provides: "Build config with React externalization"
      contains: "__SHIPSTUDIO_REACT__"
    - path: "src/index.tsx"
      provides: "Plugin entry with toolbar slot export"
      exports: ["name", "slots", "onActivate", "onDeactivate"]
    - path: "src/components/Modal.tsx"
      provides: "Modal shell with CSS injection, escape close, overlay click"
      exports: ["Modal"]
    - path: "src/views/MainView.tsx"
      provides: "Mode selector cards and disabled file picker"
      exports: ["MainView"]
    - path: "src/styles.ts"
      provides: "Injected CSS with wf2c- prefixed class names"
      exports: ["STYLE_ID", "PLUGIN_CSS"]
    - path: "dist/index.js"
      provides: "Built bundle loaded by Ship Studio"
      min_lines: 10
  key_links:
    - from: "src/index.tsx"
      to: "src/components/Modal.tsx"
      via: "import { Modal }"
      pattern: "import.*Modal.*from.*components/Modal"
    - from: "src/index.tsx"
      to: "src/views/MainView.tsx"
      via: "import { MainView }"
      pattern: "import.*MainView.*from.*views/MainView"
    - from: "src/components/Modal.tsx"
      to: "src/styles.ts"
      via: "import { STYLE_ID, PLUGIN_CSS }"
      pattern: "import.*STYLE_ID.*PLUGIN_CSS.*from.*styles"
    - from: "dist/index.js"
      to: "window.__SHIPSTUDIO_REACT__"
      via: "Vite data: URL externalization"
      pattern: "__SHIPSTUDIO_REACT__"
---

<objective>
Scaffold the complete Webflow to Code plugin — config files, source files, build output, and placeholder directories — so it loads in Ship Studio, shows a modal with mode selector preview, and has a committed dist/index.js bundle with externalized React.

Purpose: Establish the working plugin shell that all subsequent phases build on top of.
Output: A fully loadable Ship Studio plugin with visual modal shell.
</objective>

<execution_context>
@/Users/juliangalluzzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliangalluzzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffolding/01-CONTEXT.md
@.planning/phases/01-plugin-scaffolding/01-RESEARCH.md
@.planning/phases/01-plugin-scaffolding/01-VALIDATION.md

<interfaces>
<!-- Canonical reference files the executor MUST read before implementing. -->
<!-- These are the source of truth for all patterns, conventions, and exact code to copy/adapt. -->

From plugin-starter/vite.config.ts (COPY VERBATIM):
```typescript
import { defineConfig } from 'vite';
const reactDataUrl = `data:text/javascript,export default window.__SHIPSTUDIO_REACT__;export const useState=window.__SHIPSTUDIO_REACT__.useState;export const useEffect=window.__SHIPSTUDIO_REACT__.useEffect;export const useCallback=window.__SHIPSTUDIO_REACT__.useCallback;export const useMemo=window.__SHIPSTUDIO_REACT__.useMemo;export const useRef=window.__SHIPSTUDIO_REACT__.useRef;export const useContext=window.__SHIPSTUDIO_REACT__.useContext;export const createElement=window.__SHIPSTUDIO_REACT__.createElement;export const Fragment=window.__SHIPSTUDIO_REACT__.Fragment;`;
const jsxRuntimeDataUrl = `data:text/javascript,export const jsx=window.__SHIPSTUDIO_REACT__.createElement;export const jsxs=window.__SHIPSTUDIO_REACT__.createElement;export const Fragment=window.__SHIPSTUDIO_REACT__.Fragment;`;
const reactDomDataUrl = `data:text/javascript,export default window.__SHIPSTUDIO_REACT_DOM__`;
export default defineConfig({
  build: {
    lib: { entry: 'src/index.tsx', formats: ['es'], fileName: () => 'index.js' },
    rollupOptions: {
      external: ['react', 'react-dom', 'react/jsx-runtime'],
      output: { paths: { 'react': reactDataUrl, 'react-dom': reactDomDataUrl, 'react/jsx-runtime': jsxRuntimeDataUrl } },
    },
    minify: false, outDir: 'dist', emptyOutDir: true,
  },
});
```

From plugin-figma/src/context.ts (adapt for this plugin):
```typescript
import type { PluginContextValue } from './types';
const _w = window as any;
export function usePluginContext(): PluginContextValue | null {
  const React = _w.__SHIPSTUDIO_REACT__;
  const CtxRef = _w.__SHIPSTUDIO_PLUGIN_CONTEXT_REF__;
  if (CtxRef && React?.useContext) {
    return React.useContext(CtxRef) as PluginContextValue | null;
  }
  return null;
}
```

From plugin-figma/src/types.ts (PluginContextValue and supporting types):
```typescript
export interface Shell {
  exec(command: string, args: string[], options?: { timeout?: number }): Promise<{ exit_code: number; stdout: string; stderr: string }>;
}
export interface Storage {
  read(): Promise<Record<string, unknown>>;
  write(data: Record<string, unknown>): Promise<void>;
}
export interface PluginActions {
  showToast(message: string, type?: 'info' | 'success' | 'error'): void;
}
export interface PluginContextValue {
  project: { path: string; name: string };
  shell: Shell;
  storage: Storage;
  theme: { mode: 'light' | 'dark' };
  actions: PluginActions;
}
```

From plugin-figma/src/components/Modal.tsx (adapt — change class prefix from figma-plugin to wf2c):
- Modal component with CSS injection via useEffect, Escape key handler, overlay click-outside close
- Uses STYLE_ID and PLUGIN_CSS from styles.ts
- Props: open, onClose, title, headerRight?, children

From plugin-figma/src/index.tsx (pattern for toolbar button + modal toggle):
- ToolbarButton component with useState for modalOpen
- button with className="toolbar-icon-btn" and title
- Modal rendered as sibling of button
- Exports: name, slots: { toolbar: ToolbarButton }, onActivate, onDeactivate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all config and source files</name>
  <files>
    plugin.json,
    package.json,
    vite.config.ts,
    tsconfig.json,
    .gitignore,
    src/types.ts,
    src/context.ts,
    src/styles.ts,
    src/components/Modal.tsx,
    src/views/MainView.tsx,
    src/index.tsx,
    src/zip/.gitkeep,
    src/assets/.gitkeep,
    src/analysis/.gitkeep,
    src/brief/.gitkeep
  </files>
  <read_first>
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-starter/vite.config.ts,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-starter/tsconfig.json,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-starter/plugin.json,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-starter/package.json,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma/src/index.tsx,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma/src/context.ts,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma/src/styles.ts,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma/src/components/Modal.tsx,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-figma/src/types.ts,
    /Users/juliangalluzzo/Desktop/Projects/shipstudio-plugins/plugin-webflow-cloud/src/index.tsx
  </read_first>
  <action>
Create all project files following plugin-starter conventions, adapting patterns from the Figma plugin.

**Config files (copy from plugin-starter, update identity fields only):**

1. `plugin.json` — Copy plugin-starter/plugin.json verbatim, then change:
   - `"id": "webflow-to-code"`
   - `"name": "Webflow to Code"`
   - `"description": "Convert Webflow exports into structured coding briefs"`
   - `"required_commands": []` (no shell commands needed in Phase 1)
   - Keep `"slots": ["toolbar"]`, `"api_version": 1`, `"min_app_version": "0.3.53"`, `"icon": ""`

2. `package.json` — Copy plugin-starter/package.json verbatim, then change:
   - `"name": "@shipstudio/plugin-webflow-to-code"`
   - Keep all devDependencies and peerDependencies exactly as-is

3. `vite.config.ts` — Copy plugin-starter/vite.config.ts VERBATIM. Do not modify any line. The data: URL aliases for React externalization are critical and fragile.

4. `tsconfig.json` — Copy plugin-starter/tsconfig.json VERBATIM. No modifications.

5. `.gitignore` — Create with these entries:
   ```
   node_modules/
   .DS_Store
   ```
   IMPORTANT: Do NOT add `dist/` to .gitignore. dist/index.js must be committed.

**Source files:**

6. `src/types.ts` — Copy the PluginContextValue interface and its supporting types (Shell, Storage, PluginActions) from plugin-figma/src/types.ts. Do NOT copy Figma-specific types (FigmaUrlParts, ExtractionScope, FigmaUser, FigmaFileInfo). Keep only the 4 interfaces needed by any Ship Studio plugin.

7. `src/context.ts` — Copy plugin-figma/src/context.ts verbatim. It imports from `./types` and uses `__SHIPSTUDIO_PLUGIN_CONTEXT_REF__` (the correct modern pattern). Do NOT use the older `__SHIPSTUDIO_PLUGIN_CONTEXT__` pattern from webflow-cloud.

8. `src/styles.ts` — Create with:
   - `export const STYLE_ID = 'webflow-to-code-styles';`
   - `export const PLUGIN_CSS = \`...\`;` containing CSS rules adapted from plugin-figma/src/styles.ts
   - Change ALL class name prefixes from `figma-plugin-` to `wf2c-` throughout the CSS string
   - Include at minimum these classes (adapted from the Figma plugin CSS):
     - `.wf2c-overlay` — `position: fixed; inset: 0; background: rgba(0,0,0,0.6); backdrop-filter: blur(2px); display: flex; align-items: center; justify-content: center; z-index: 10000;`
     - `.wf2c-modal` — `width: 480px; max-height: 80vh; border-radius: 12px; overflow: hidden; display: flex; flex-direction: column; background: var(--bg-primary); color: var(--text-primary); border: 1px solid var(--border);`
     - `.wf2c-modal-header` — `display: flex; flex-direction: row; padding: 16px 20px; border-bottom: 1px solid var(--border); gap: 12px; align-items: center;`
     - `.wf2c-modal-body` — `padding: 20px; overflow-y: auto; flex: 1; scrollbar-width: none; -ms-overflow-style: none;`
     - `.wf2c-modal-body::-webkit-scrollbar` — `display: none;`
     - `.wf2c-modal-title` — `font-size: 15px; font-weight: 600;`
     - `.wf2c-label` — `font-size: 12px; font-weight: 500; color: var(--text-secondary); margin-bottom: 6px; display: block;`
     - `.wf2c-mode-group` — `display: flex; flex-direction: column; gap: 4px;`
     - `.wf2c-mode-card` — `padding: 6px 8px; border-radius: 4px; background: var(--bg-secondary); border: 1px solid var(--border); cursor: pointer; transition: border-color 0.15s ease;`
     - `.wf2c-mode-card:hover` — `border-color: var(--text-muted);`
     - `.wf2c-mode-card.selected` — `border-color: var(--accent, #0d99ff);`
     - `.wf2c-mode-card-name` — `font-size: 11px; font-weight: 600; color: var(--text-primary); line-height: 1.3;`
     - `.wf2c-mode-card-desc` — `font-size: 10px; color: var(--text-muted); line-height: 1.3;`
     - `.wf2c-btn-primary` — `width: 100%; padding: 8px 12px; border-radius: 6px; background: var(--accent, #0d99ff); color: white; border: none; font-size: 13px; cursor: pointer;`
     - `.wf2c-btn-primary:disabled` — `opacity: 0.5; cursor: not-allowed;`

9. `src/components/Modal.tsx` — Adapt plugin-figma/src/components/Modal.tsx:
   - Import `{ STYLE_ID, PLUGIN_CSS }` from `'../styles'`
   - Import `{ useEffect, useCallback }` from `'react'` and `type { ReactNode, MouseEvent }` from `'react'`
   - Same ModalProps interface: `{ open, onClose, title, headerRight?, children }`
   - Same three behaviors: CSS injection useEffect, Escape key useEffect, overlay click handler
   - Change ALL class names from `figma-plugin-*` to `wf2c-*` (matching styles.ts):
     - `figma-plugin-overlay` -> `wf2c-overlay`
     - `figma-plugin-modal` -> `wf2c-modal`
     - `figma-plugin-modal-header` -> `wf2c-modal-header`
     - `figma-plugin-modal-title` -> `wf2c-modal-title`
     - `figma-plugin-modal-body` -> `wf2c-modal-body`
   - In the header, render the Webflow 'W' logo SVG instead of the Figma logo. The Webflow SVG is: `<svg width="16" height="16" viewBox="0 0 1080 674" fill="currentColor"><path fillRule="evenodd" clipRule="evenodd" d="M1080 0L735.385 673.684H411.695L555.915 394.481H549.444C430.463 548.934 252.941 650.61 -0.000976562 673.684V398.344C-0.000976562 398.344 161.812 388.787 256.938 288.776H-0.000976562V0.0053214H288.77V237.515L295.252 237.489L413.254 0.0053214H631.644V236.009L638.125 235.999L760.555 0H1080Z" /></svg>`

10. `src/views/MainView.tsx` — Create the mode selector preview:
    - Import `{ useState }` from `'react'`
    - Define `type Mode = 'pixel-perfect' | 'best-site';`
    - Export `function MainView()`
    - Use `useState<Mode>('pixel-perfect')` for selected mode
    - Render a "Conversion Mode" label with className `wf2c-label`
    - Render two mode cards in a `wf2c-mode-group` div:
      - "Pixel Perfect" card: name="Pixel Perfect", desc="Exact dimensions, fixed units, preserve Webflow layout"
      - "Best Site" card: name="Best Site", desc="Semantic HTML, responsive patterns, modern conventions"
    - Each card: `className={wf2c-mode-card${selected ? ' selected' : ''}}`, onClick sets mode
    - Render a disabled button: `<button className="wf2c-btn-primary" disabled>Select Webflow Export (.zip)</button>`

11. `src/index.tsx` — Plugin entry point:
    - Import `{ useState }` from `'react'`
    - Import `{ Modal }` from `'./components/Modal'`
    - Import `{ MainView }` from `'./views/MainView'`
    - Create `WebflowIcon` component returning the Webflow 'W' SVG with `width="14" height="14" viewBox="0 0 1080 674" fill="currentColor"` and the same path as in Modal.tsx
    - Create `ToolbarButton` component:
      - `const [modalOpen, setModalOpen] = useState(false);`
      - Return fragment with:
        - `<button onClick={() => setModalOpen(true)} title="Webflow to Code" className="toolbar-icon-btn"><WebflowIcon /></button>`
        - `<Modal open={modalOpen} onClose={() => setModalOpen(false)} title="Webflow to Code"><MainView /></Modal>`
    - Export `const name = 'Webflow to Code';`
    - Export `const slots = { toolbar: ToolbarButton };`
    - Export `function onActivate() { console.log('[webflow-to-code] Plugin activated'); }`
    - Export `function onDeactivate() { console.log('[webflow-to-code] Plugin deactivated'); }`

**Placeholder directories:**

12. Create empty `.gitkeep` files in: `src/zip/`, `src/assets/`, `src/analysis/`, `src/brief/`
  </action>
  <verify>
    <automated>ls plugin.json package.json vite.config.ts tsconfig.json .gitignore src/index.tsx src/types.ts src/context.ts src/styles.ts src/components/Modal.tsx src/views/MainView.tsx src/zip/.gitkeep src/assets/.gitkeep src/analysis/.gitkeep src/brief/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - plugin.json contains `"id": "webflow-to-code"` and `"api_version": 1` and `"slots": ["toolbar"]`
    - package.json contains `"name": "@shipstudio/plugin-webflow-to-code"` and `"vite": "^6.0.0"` in devDependencies
    - vite.config.ts contains `__SHIPSTUDIO_REACT__` and `data:text/javascript`
    - tsconfig.json contains `"jsx": "react-jsx"` and `"moduleResolution": "bundler"`
    - .gitignore contains `node_modules/` and does NOT contain `dist`
    - src/index.tsx contains `export const name = 'Webflow to Code'` and `export const slots`
    - src/types.ts contains `export interface PluginContextValue`
    - src/context.ts contains `__SHIPSTUDIO_PLUGIN_CONTEXT_REF__` (NOT `__SHIPSTUDIO_PLUGIN_CONTEXT__` without REF)
    - src/styles.ts contains `export const STYLE_ID = 'webflow-to-code-styles'` and `wf2c-overlay` and `wf2c-modal` and `wf2c-mode-card`
    - src/components/Modal.tsx contains `wf2c-overlay` and `wf2c-modal` and `export function Modal`
    - src/views/MainView.tsx contains `export function MainView` and `Pixel Perfect` and `Best Site` and `wf2c-mode-card` and `Select Webflow Export`
    - All 4 placeholder .gitkeep files exist
  </acceptance_criteria>
  <done>All 15 files created. Plugin structure matches plugin-starter conventions with wf2c-prefixed styles, Webflow icon, mode selector cards, and disabled file picker in MainView.</done>
</task>

<task type="auto">
  <name>Task 2: Install dependencies, build bundle, and run smoke checks</name>
  <files>dist/index.js</files>
  <read_first>
    package.json,
    vite.config.ts,
    src/index.tsx
  </read_first>
  <action>
1. Run `npm install` in the project root to install devDependencies (vite, typescript, @types/react).

2. Run `npm run build` to produce `dist/index.js` via Vite.

3. Run smoke checks to verify the bundle:
   - `node -e "const b=require('fs').readFileSync('dist/index.js','utf8'); if(!b.includes('__SHIPSTUDIO_REACT__')) { console.error('FAIL: React not externalized'); process.exit(1); } if(b.length > 100000) { console.error('FAIL: Bundle too large:', b.length); process.exit(1); } console.log('PASS: Bundle size:', b.length, 'bytes, React externalized');"` — verifies React is externalized and bundle is small
   - `node -e "const m=JSON.parse(require('fs').readFileSync('plugin.json','utf8')); if(m.id!=='webflow-to-code') { console.error('FAIL: wrong id:', m.id); process.exit(1); } if(m.api_version!==1) { console.error('FAIL: wrong api_version'); process.exit(1); } if(!m.slots.includes('toolbar')) { console.error('FAIL: missing toolbar slot'); process.exit(1); } console.log('PASS: plugin.json valid');"` — verifies plugin.json
   - `grep -c "createElement" dist/index.js` — should be 0 or very low (no bundled React createElement)

4. If build fails, fix TypeScript errors and rebuild. Common issues:
   - Missing type import: ensure `import type { ReactNode, MouseEvent } from 'react'` in Modal.tsx
   - JSX runtime: ensure tsconfig has `"jsx": "react-jsx"`

5. After successful build and smoke checks, stage dist/index.js for commit. Do NOT add node_modules.
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs'); const b=fs.readFileSync('dist/index.js','utf8'); if(!b.includes('__SHIPSTUDIO_REACT__')||b.length>100000) process.exit(1); const m=JSON.parse(fs.readFileSync('plugin.json','utf8')); if(m.id!=='webflow-to-code'||m.api_version!==1||!m.slots.includes('toolbar')) process.exit(1); console.log('All smoke checks passed');"</automated>
  </verify>
  <acceptance_criteria>
    - `dist/index.js` exists and is non-empty
    - `dist/index.js` contains `__SHIPSTUDIO_REACT__` (React externalized via data: URLs)
    - `dist/index.js` is less than 100KB (no bundled React)
    - `npm run build` exits with code 0 (no TypeScript or Vite errors)
    - plugin.json passes JSON parse with id=webflow-to-code, api_version=1, slots includes toolbar
  </acceptance_criteria>
  <done>dist/index.js is built, passes all smoke checks (React externalized, bundle under 100KB, plugin.json valid), and is ready to commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify plugin loads in Ship Studio</name>
  <files>none</files>
  <action>
User manually verifies the plugin loads and renders correctly in Ship Studio. All automated work is complete — this checkpoint confirms visual and interactive behavior that cannot be tested outside the host app.
  </action>
  <verify>User confirms all 11 verification steps pass</verify>
  <done>Plugin confirmed working in Ship Studio — toolbar icon renders, modal opens with mode cards and disabled file picker, no console errors.</done>
  <what-built>Complete Webflow to Code plugin shell — toolbar button with Webflow icon, modal with mode selector cards (Pixel Perfect / Best Site) and disabled file picker button. All config files, source files, and committed dist/index.js bundle.</what-built>
  <how-to-verify>
    1. Open Ship Studio
    2. Go to Settings > Plugins > "Link Dev Plugin"
    3. Enter the path: the project root directory
    4. Verify: Webflow 'W' icon appears in the toolbar
    5. Click the toolbar icon
    6. Verify: Modal opens with "Webflow to Code" title and Webflow logo in header
    7. Verify: Two mode cards visible — "Pixel Perfect" and "Best Site" — clicking each highlights it with accent border
    8. Verify: "Select Webflow Export (.zip)" button is visible but disabled (grayed out, not clickable)
    9. Press Escape — verify modal closes
    10. Reopen modal, click the dark overlay area — verify modal closes
    11. Open DevTools (Cmd+Option+I) — verify no console errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All files exist: `ls plugin.json package.json vite.config.ts tsconfig.json .gitignore src/index.tsx src/types.ts src/context.ts src/styles.ts src/components/Modal.tsx src/views/MainView.tsx`
2. Build succeeds: `npm run build` exits 0
3. Bundle is externalized: `dist/index.js` contains `__SHIPSTUDIO_REACT__` and is under 100KB
4. Plugin manifest is valid: plugin.json has id=webflow-to-code, api_version=1, toolbar slot
5. No bundled React: `grep -c "createElement" dist/index.js` returns 0 or very low count
6. CSS namespace is correct: `grep "wf2c-" src/styles.ts` shows prefixed class names
7. Context hook uses modern pattern: `grep "__SHIPSTUDIO_PLUGIN_CONTEXT_REF__" src/context.ts` returns a match
</verification>

<success_criteria>
- dist/index.js exists, is committed to git, contains __SHIPSTUDIO_REACT__ references, and is under 100KB
- Plugin loads in Ship Studio without errors when linked as dev plugin
- Modal opens with Webflow logo, mode selector cards, and disabled file picker
- All CSS classes use wf2c- prefix (no figma-plugin- prefix pollution)
- Placeholder directories exist for future phases (src/zip/, src/assets/, src/analysis/, src/brief/)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffolding/01-01-SUMMARY.md`
</output>
